# PAPP Conversions

**State (read 2026-09-11 at `3b5bcd2`): greenfield. There is no application code yet.**

Repository: [NonaSuomy/papp-conversions](https://github.com/NonaSuomy/papp-conversions) (public, default branch `main`), created 2026-09-11 by Nona Suomy (host). Franklin is a member.

What is in the repository:

| Path | What it is |
|---|---|
| `README.md` | Title only: "PAPP Conversions". |
| `docs/overview.md` | Mirror of this hub memory file (revision 1, placeholder text). |
| `docs/shared-memory/manifest.json` | Hub bookkeeping: maps memory files to `docs/*.md` with hashes. |

Nothing else: no package manifest, no source, no tests, no CI, no `AGENTS.md`/`CLAUDE.md`, no issues, no PRs. The hub recorded the connection as `existingCodebase: false`.

What we do not know yet: what "PAPP" is, what is being converted (from what, to what, for whom), and what the finished product looks like. The pinned plan is empty. See **openQuestions**.

Next step: the hosts describe the goal (`/plan <one sentence>` in General is enough), then kickoff: questions, one proposal with 2-3 approaches, decision, tasks.
