--- a/frontends/framebuffer/fbtk/text.c
+++ b/frontends/framebuffer/fbtk/text.c
@@ -70,6 +70,8 @@ fb_text_font_style(fbtk_widget_t *widget, int *font_height, int *padding,
 	*padding = (widget->height - *padding - *font_height) / 2;
 #endif
 
+	/* no font-family names: the PAPP font backend reads them */
+	font_style->families = NULL;
 	font_style->family = PLOT_FONT_FAMILY_SANS_SERIF;
 	font_style->size = px_to_pt(*font_height * PLOT_STYLE_SCALE);
 	font_style->weight = 400;
